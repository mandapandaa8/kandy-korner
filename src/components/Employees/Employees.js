export const Employees = ({ name, location }) => {
    return <section className="employee" >
        <div>
        Name: {name}
        </div>
        <div>Location: {location}</div>
    </section>
}